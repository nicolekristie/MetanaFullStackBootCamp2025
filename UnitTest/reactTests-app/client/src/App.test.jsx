// import supertest from 'supertest'
// import App from 


//need to add in the endpoint res.sendStatus(200)

describe("POST /employee", () => {
    it('should ', () => {
        
    });("should respond with a 200 status code"), async () => {
        const response = await request("test").post("/users").send()
        console.log(response)
    }

})